image: python:3.10
job_name: test

setup: |
  uname -a
  echo "hello world"
  python3 --version
  pip3 install -r requirements.txt

run: |
  echo "hello world" 

profile: |
  echo "hello world, profile" 
